cin >> n;
    // ll Max;
    // memset(f,0,sizeof f);
    // for (int i = 1; i <= n; i++)
    // {
    //     cin >> a[i].r >> a[i].h;
    //     // Max=max(Max,a[i].r);
    //     // f[a[i].r]=a[i].h;
    // }